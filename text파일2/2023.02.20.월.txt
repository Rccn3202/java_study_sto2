2023.02.20.월
트리구조
검색을 효과적으로 하고싶다
비교조건 명시해야한다(
1.필드 모아놓은 클래스 변경 안하고 비교 사용
2.필드 모아놓은 클래스 변경-추상메소드 오버라이드

동등비교
set
같은 것은 1개만 넣어준다
순서가 없다
1. hashcode() (주소) 같아야한다
2. equals()까지 같아야 동등객체

class 1 : main() { a1(); }
class 2: a1() { a2(); }
일 때
p676
LIFO(스택구조)
[a2()]
[a1()]
[main]
이렇게 쌓여있다.(스택구조) 
a2-a1-main 순서대로 빠져나간다. 
주로 함수를 차례대로 처리 할 때 사용
4칙연산에서 복잡한 것을 처리함


FIFO(큐 구조) (선입선출)
>a2() a1() main()<
이렇게 되어있다.
먼저 들어간게 먼저 나간다.
네명이서 프린터 하나 공유할 때, 프린터 인쇄 순서 등
(먼저 명령한 사람 것이 먼저 인쇄됨)
입출력에서 데이터 순서 기억해주는 메모리-채널
차례차례
캐쉬메모리, 버퍼메모리

캐쉬메모리
데이터량이 많은 프로그램은 전부 램에 올려 돌리지 않고, 일부 게임 시작할 때 프로그램만 램(주기억)
나머지는 하드디스크에 저장. 
주기억장치에서 찾고 없으면 하드디스크에서 찾는다
주기억장치 전에 캐쉬메모리(용량 많지 않지만 자주쓰는 것 데이터 넣어놓음/데이터 빨리 찾을 수 있음)
cpu에게 검색 요청하면 캐쉬-주기억-하드디스크 순서로 검색

버퍼메모리(ctrl+c 했을 때 자료가 저장되어있음/ctrl+v 출력됨)

데큐 구조(스택+큐)
문이 양쪽에 있음
프로그램으로 한쪽 문을 막으면 스택처럼 사용 가능 >=ㅣ
입출 문 막으면 큐처럼 사용가능
스택,큐 2개를 활용하기 위한 구조

데이터 넣기 push
데이터 빼기 pop
==2=========================
p.687
1-4//선입선출은 queue
2-2//
3-4
4-4
5-
6-Set<String,Integer> s=new Set<>();
7. 

==3=============
보통 컴퓨터는 cpu 4개
cpu(프로세서):중앙처리장치
컴퓨터 5대장치 : 입력(키보드,마우스) 출력(모니터) 연산 제어 기억 (중앙처리장치:연산제어기억)

멀티스레드 : 나눠서 일 처리
싱글스레드 : 혼자 모든 일 처리(cpu 1개)


token 에러는 동작 명령어를 메소드에 안넣었을 때 나온다


Outputs all lists that match the values entered in the arraylist

